var tstart, tend, nst
tstart = date('12-8-2 06:00')
tend = date('12-8-2 06:30')
nst = 'node-06-[01-16]'
out = each v in cpu_user 
# where v.node ins nst and v.time < tend and v.time >= tstart
		yield node => v.node, cpu => v.value
end

print out
